include ../../config/Xfm-demos.rules

# Makefile used to build the xfm examples

	      PR1 = Multi
         PROGRAMS = $(PR1)

.PHONY: $(PROGRAMS)

all:: $(PROGRAMS)

$(PR1):    $(PR1).fm $(PR1)Make $(PR1)App.c $(PR1)App.h $(PR1)Main.c $(PR1).exe $(PR1).editor
