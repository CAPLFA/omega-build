/*
 * 
 * This file was generated by +PNAME+ from the file +SNAME+
 * It will require some changes to reflect application
 * specific requirements.
 */

/*
 * Includes
 */
#include <Fm.h>
#include "+UNAME+.h"

Widget Toplevel;

/*
 * Active values
 */

+AVDECL+


void	main(argc, argv)
int argc; 
char **argv;
{
#ifdef CCODE
	extern Widget FmCreate+UNAME+_fm();
#endif
		
	Widget toplevel = FmInitialize( "+LNAME+", "+UNAME+", NULL, 0, &argc, argv );
#ifdef CCODE
	Toplevel = FmCreate+UNAME+_fm( "+LNAME+", toplevel, NULL, 0 );
#else

+FUNCATTACH+

	Toplevel = FmLoadCreate( "+LNAME+", "+UNAME+.fm", toplevel, NULL, 0 );
#endif    

+ACTIVEVALUES+
    
	FmLoop();
}
    
/********************** end of ${pname}.c *******************************/

